## Última actualización

- Se configuró el despliegue automático con GitHub Actions.
- La imagen Docker se publica en GitHub Container Registry (GHCR).
- La aplicación se despliega automáticamente en el VPS mediante Docker Swarm.
- Configuración preparada para el subdominio `yajanuha.byronrm.com`.